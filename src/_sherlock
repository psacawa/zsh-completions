#compdef sherlock

# AUTOMATCALLY GENERATED by `shtab`

_shtab_sherlock_py_options_=(
  "(- :)"{-h,--help}"[show this help message and exit]"
  "--completions[print shell completion script]:completions:(bash zsh)"
  "(- :)--version[Display version information and dependencies.]"
  {--verbose,-v,-d,--debug}"[Display extra debugging information and metrics.]"
  {--folderoutput,-fo}"[If using multiple usernames, the output of the results will be saved to this folder.]:folderoutput:"
  {--output,-o}"[If using single username, the output of the result will be saved to this file.]:output:"
  {--tor,-t}"[Make requests over Tor\; increases runtime\; requires Tor to be installed and in system path.]"
  {--unique-tor,-u}"[Make requests over Tor with new Tor circuit after each request\; increases runtime\; requires Tor to be installed and in system path.]"
  "--csv[Create Comma-Separated Values (CSV) File.]"
  "*--site[Limit analysis to just the listed sites. Add multiple options to specify more than one site.]:site_list:"
  {--proxy,-p}"[Make requests over a proxy. e.g. socks5\:\/\/127.0.0.1\:1080]:proxy:"
  {--json,-j}"[Load data from a JSON file or an online, valid, JSON file.]:json_file:"
  "--timeout[Time (in seconds) to wait for response to requests. Default timeout is infinity. A longer timeout will be more likely to get results from slow sites. On the other hand, this may cause a long delay to gather all results.]:timeout:"
  "--print-all[Output sites where the username was not found.]"
  "--print-found[Output sites where the username was found.]"
  "--no-color[Don\'t color terminal output]"
  {--browse,-b}"[Browse to all results on default browser.]"
  {--local,-l}"[Force the use of the local data.json file.]"
)

_shtab_sherlock_py_commands_() {
  local _commands=(
    
  )

  _describe 'sherlock.py commands' _commands
}


typeset -A opt_args
local context state line curcontext="$curcontext"

_arguments \
  $_shtab_sherlock_py_options_ \
  "(*):One or more usernames to check with social networks.:" \
  ': :_shtab_sherlock_py_commands_' \
  '*::args:->args'

case $words[1] in
  
esac
